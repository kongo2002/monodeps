dependencies:
  - shared_code:
      path: ../shared

dependency_overrides:
  some_lib:
    path: ../libs/some_lib

flutter:
  assets:
    - ../directory/

fonts:
  - family: Raleway
    fonts:
      - asset: ../fonts/Raleway.ttf
